package dgs1sdt.pie.metrics.createTarget;

import org.springframework.data.jpa.repository.JpaRepository;

public interface MetricCreateTargetRepository extends JpaRepository<MetricCreateTarget, Long> {
}
