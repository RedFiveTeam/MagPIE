package dgs1sdt.pie.metrics.deleteTarget;

import org.springframework.data.jpa.repository.JpaRepository;

public interface MetricDeleteTargetRepository extends JpaRepository<MetricDeleteTarget, Long> {
}
