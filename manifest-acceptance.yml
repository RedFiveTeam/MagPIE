---
applications:
  - name: pie-acceptance
    buildpacks: https://github.com/cloudfoundry/java-buildpack.git
    memory: 1GB
    instances: 1
    stack: cflinuxfs3
    path: target/pie-0.0.1-SNAPSHOT.jar
    environment:
      - PIE_DB_URL=jdbc:mysql://localhost:3306/fritzdev?useSSL=false
      - PIE_DB_USERNAME=pie
    services:
      - pie-acceptance-db

